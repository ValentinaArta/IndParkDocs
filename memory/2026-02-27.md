# 2026-02-27 — Утренняя сессия

## Mobile fixes (iPhone 14 Pro)
- `viewport-fit=cover` + `100dvh` + safe-area-inset padding
- sidebar `.open`: width 280px, max-width 85vw
- Async view race condition fix: `currentView` guard после каждого `await` в showDashboard/showEntity/showEntityList — предотвращает перезапись контента при навигации

## Metabase BI
- Metabase уже развёрнут: `benthic-hull.metabaseapp.com`
- Публичный дашборд: `17e56021-56da-4579-a085-e31dde6c0b7c` (dashboard 12)
- Добавлены 2 диаграммы площадей:
  - Card 120: "Площади: сдано / свободно (общее)" — pie
  - Card 119: "Площади: сдано / всего по корпусам" — stacked bar
- SQL использует `total_area` из properties корпусов (не из помещений!)
- Фильтр внешней аренды: `contractor_name NOT IN (SELECT name FROM own_companies WHERE is_own='true')`
- Fuzzy match building names: `regexp_replace(name, '\s*\(.*\)', '')` для нормализации
- Dashboard 13 "Аренда по корпусам — детали" (uuid: `51f6a6bd-5119-4867-801c-9f0b42991608`) — таблица договоров с click → external URL
- Custom click на bar chart → dashboard 13
- Card 121: таблица договоров аренды по корпусам

## URL routing
- `#entity/ID` → открывает карточку сущности
- `window.addEventListener('hashchange', ...)` для навигации из Metabase
- Regex через `new RegExp('^#entity/(\\d+)')` (один backslash в template literal → правильный `\d` в HTML)

## Drill-down пирог на фронтенде (SVG donut)
- 3 уровня: total → buildings → contracts (по договорам внутри корпуса)
- Клик по сектору договора → `showEntity(contract_id)`
- Бэкенд `/reports/area-stats` расширен: возвращает `contracts[]` внутри каждого building
- `_svgDonut()`, `_areaDrill()`, `_renderAreaChart()`, `_areaLevel`/`_areaSelectedBuilding`

## Форма договора аренды — исправления
1. Убраны "Периодичность оплаты" и "Типы предметов КП" для Аренды/Субаренды
2. Площадь автозаполняется при выборе помещения (`onRoRoomSelect` → `room.properties.area`)
3. Чекбоксы "Доп. услуги" и "Аренда внешняя" — `inline-flex` + `gap:6px`
4. НДС — убран дубль (оставлен только в rent section)
5. Роль + компания на одной строке (grid 160px + 1fr)
6. **Новое поле**: ☑ "Выделена эл. мощность" → "Эл. мощность по договору (ДС), кВт" (number)
   - properties: `has_power_allocation`, `power_allocation_kw`
   - Сбор в `collectDynamicFieldValues` + `onRentFieldChange`

## ДС (доп. соглашения)
- Убран дубль номера: `s.name` уже содержит "ДС №16", не добавлять ` №16` повторно
- Срок действия всегда раскрыт (forceOpen=true для supplement)

## Контактные лица компаний
- field_type `contacts` — JSON массив `[{name, position, phone, email}]`
- Старые поля (contact_person, phone, email) скрыты (sort_order=999)
- `_renderContactsList()`, `_addContact()`, `_removeContact()`, `_collectContacts()`
- Миграция данных: 7 компаний перенесены из старых полей в `contacts`
- Карточка: отображение с иконками 📞 ✉

## Исправления формы (утро, продолжение)

### Карточка договора аренды
- `power_allocation_kw` добавлен в ответ `/api/reports/contract-card/:id`
- На карточке: `⚡ Эл. мощность: X кВт` после ежемесячного платежа

### Роли на 2 строках (edit modal)
- Edit modal дублировал логику создания — роли были на 4 строках
- Применён тот же grid-паттерн: `grid-template-columns:160px 1fr`
- `our_role_label` рендерится вместе с `our_legal_entity` (defer pattern)

### Убраны лишние поля из ДС аренды
- `payment_frequency`, `sale_item_type` — return в edit modal для rental types
- `vat_rate` — return для rental (рендерится в rent section)
- Duration fields для non-supplement скрыты (а для supplement — показаны как обычные поля)

### «Аренда внешняя» — автоопределение
- Галочка убрана из UI, заменена на `<input type="hidden" id="f_external_rental" value="auto">`
- В `collectDynamicFieldValues`: проверяет `_ownCompanies` — если контрагент наш → false, иначе true
- Сохраняет `external_rental: "true"/"false"` автоматически

### Чекбоксы — nowrap
- `white-space:nowrap` на label для "Выделена эл. мощность" и "Доп. услуги"

### Порядок полей объекта аренды
- Было: Тип помещения → Корпус → Помещение
- Стало: Корпус → Помещение → Тип помещения (скрыт, показывается после автозаполнения)
- `onRoRoomSelect` теперь автозаполняет: корпус (parent_id), тип (object_type), площадь (area)
- `ro_type_wrap_N` — div обёртка для типа, `display:none` пока пустой

## Вечерняя сессия

### Приветствие на дашборде
- Добавлено приветствие на главной странице
- `40f7ebb` — greeting on dashboard

### Пасхалки / шутки
- `ea1fdc9` — "get well soon Nastyusha" (пожелание выздоровления)
- `9e9f243` — "Pasha is the coolest"

## Коммиты (dev→main)
- `894adce` — async view race condition fix
- `1b6ab24` — interactive area pie chart 3-level drill-down
- `c416ca8` — URL routing #entity/ID
- `58354b6` — hide payment_frequency/sale_item_type, auto-fill area, checkbox layout
- `7c56aaa` — electrical power allocation
- `70ec6e5` — duration fields always open for supplements
- `2e21126` — fix duplicate supplement number
- `ba88f22` — role label + entity on same row
- `3baa7ca` — multiple contacts with position
- `0e6c472` — hide duplicate VAT for rent
- `6946b0c` — no duplicate duration section in supplement form
- `b98ef0a` — power on card, roles grid in edit, hide rent fields in supp, auto external_rental
- `353f041` — reorder rent object fields: room first, auto-fill building+type+area
- `40f7ebb` — greeting on dashboard
- `ea1fdc9` — get well soon Nastyusha
- `9e9f243` — Pasha is the coolest
