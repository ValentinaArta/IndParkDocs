# 2026-03-03

## Последние коммиты (хронологически)
- `390b909` — act edit modal
- `e57a5dd` — acts in contract history
- `c3e9e0f` — company split (Наши/Сторонние/Из 1С)
- `b390c55` — 1С contract numbers in overdue
- `56880cb` — Должники expandable contractor rows
- `245f06b` — Расходы expandable contractor rows
- `d5a69ca` — searchable-select regex fix (`\d` → `\\d`)
- `df1ca68` — test suite (94 tests) + pre-deploy script
- **nav rename** (Расходы/Должники, no DEMO badge) — задеплоен на dev + main
- `923aee7` — cadastral_number required for land_plot_part
- `48910cd` — rent object name fix (ЗУ → land_plot_part_name)
- `48aebf7` — finance page state preservation (_expOpenGids)

## Текущий HEAD (оба ветки dev + main)
`48aebf7` — finance page state preservation

## Что сделано сегодня (2026-03-03)
1. ✅ **Должники** — expandable contractor rows + contract breakdown
2. ✅ **Расходы** — expandable contractor rows с data-gid (обход `\'` бага)
3. ✅ **Frontend разбит на модули** — `server/src/frontend/` (16 файлов)
4. ✅ **searchable-select.js** — regex fix `(d+)` → `(\\d+)` в `_srchPick()` — area auto-fill заработал
5. ✅ **Тест-сьют** — 94 tests, 8 suites; `tests/__mocks__/otplib.js`; Jest config в package.json
6. ✅ **pre-deploy.sh** — 7 проверок (syntax, JS, Jest, audit, .env, secrets, git-clean)
7. ✅ **Nav rename** — "Расходы" + "Должники", убран DEMO badge
8. ✅ **cadastral_number required** — migration 024, валидация в create/edit, auto-fill из родит. ЗУ
9. ✅ **Rent object name fix** — ЗУ теперь показывает land_plot_part_name || land_plot_name
10. ✅ **Finance state preservation** — _expOpenGids Set; раскрытые строки сохраняются при рефреше

## 🔄 В ПРОЦЕССЕ (не завершено — текущая задача при компакте)
### Supplement card (ДС) — стилизованная карточка как у договора
- **Цель**: сделать карточку ДС визуально похожей на `renderContractCard`
- **Файл**: `server/src/frontend/entity-crud.js`
- **Паттерн**: в `showEntity()` (~line 2185) добавить перехват для `supplement` type (как сделано для `contract`)
- **Что нужно**:
  1. В `showEntity` добавить `_isSupp = (e.type_name === 'supplement')`, кнопку "⚙ Детали"
  2. Добавить `if (_isSupp && !_forceDetail)` блок → вызывать `renderSupplementCard(e)` и return
  3. Реализовать `renderSupplementCard(supp)` функцию рядом с `renderContractCard`

- **`renderSupplementCard(supp)` структура**:
  - Header: contractor (из supp.properties или supp.parent), "ДС №", дата
  - Subtitle: "Доп. соглашение к договору [type]"
  - Ссылка на родительский договор (supp.parent.id / supp.parent.name)
  - Стороны: Арендодатель / Арендатор (наследуются или из свойств ДС)
  - Блок "Что изменилось": sp.changes_description (с accent-border)
  - Таблица "Изменённые условия аренды": sp.rent_objects JSON → синяя шапка как у renderContractCard
  - Поля: contract_date, contract_end_date, contract_amount, срок и т.д.

- **Свойства ДС** (ключи в `supp.properties`):
  - `contract_type`, `changes_description`, `rent_objects` (JSON массив)
  - `contract_date`, `contract_end_date`, `contract_amount`, `number`
  - `rent_objects` строки: `{ room_name, object_type, area, rent_rate, fixed_rent }`

- **Где брать данные**:
  - `supp.parent` → из `/entities/:id` API возвращает direct parent
  - `supp.properties` → как в любом entity
  - Наследовать стороны из parent или из supp.properties

- **Ключевые линии в entity-crud.js**:
  - `renderContractCard` — ~line 4438 (образец)
  - `showEntity` contract intercept — lines 2221–2232 (паттерн для replicate)
  - `openSupplementCard(id)` — ~line 4661 (modal контекст)
  - Хелперы: `_ccFmtDate`, `_ccFmtNum`

- **Решение (no new API)**: только client-side из `/entities/:id` данных — parent + properties

## После создания договора → автооткрытие карточки
- **Файл**: `server/src/frontend/entity-crud.js`
- Нужно найти в POST-ответе где вызывается `showEntityList(type)` → заменить на `showEntity(newId)`
- Сервер возвращает новый entity ID в ответе POST /api/entities
- **НЕ завершено**

## Архитектура frontend (модули)
```
server/src/frontend/
├── index.js              — точка входа, сборка HTML
├── core.js               — startApp, globals, routing
├── css.js                — все стили
├── layout.js             — sidebar, nav, shell
├── entity-crud.js        — CRUD: create/edit/delete/show modals
├── entity-form.js        — рендер полей форм
├── acts.js               — форма/рендер актов
├── ai-chat.js            — AI чат виджет
├── land-plot-parts.js    — части ЗУ
├── relations.js          — управление связями
├── rent-objects.js       — объекты аренды в форме договора
├── reports.js            — отчёты, карточки договоров
├── searchable-select.js  — searchable dropdown
├── settings.js           — настройки, справочники
└── supplements.js        — доп. соглашения
```

## Тесты
- `server/tests/` — 8 файлов: health, auth, entities, relations, reports, finance, ai-chat, frontend
- Mock pattern: `const mockQuery = jest.fn().mockResolvedValue({ rows: [] }); jest.mock('../src/db', ...)`
- `tests/__mocks__/otplib.js` — ESM mock (необходим!)
- `tests/setup.js` — env vars (ODATA_BASE_URL=http://127.0.0.1:19999 и др.)
- Запуск: `cd server && npm test`

## Pre-deploy
- `bash scripts/pre-deploy.sh` от корня репо
- Проверки 1-6 блокируют деплой, проверка 7 (git clean) — advisory

## Финансы/Бюджет
- `/budget` — Blended forecast dashboard (iframe), locked to ЦФО "ИП"
- `/finance` — overdue invoices (Должники), с номерами договоров 1С
- `/api/finance/summary` — summary SPA (Расходы)
- Drill-down аренды: 8 контрагентов возвращается
- budget_data: 2360 rows, файлы `/tmp/budget_2026_bdds.xlsx` и `/tmp/budget_2026_bdr.xlsx`

## AI Chat
- `routes/ai-chat.js` — inline Anthropic, claude-haiku-4-5, SQL tool, 10 msg history
- ANTHROPIC_API_KEY в server/.env (не в git)

## Contacts field (НЕ РЕАЛИЗОВАН рендер)
- Поля contact_person/phone/email скрыты (sort_order=999)
- Поле `contacts` (field_type='contacts') существует в field_definitions
- Рендер, сбор и отображение — не написаны
- Нужно: рендер (~line 1186 entity-form.js), collect, display на карточке

## VPN
- ppp0 10.253.190.14, 192.168.2.3 (1С)
- Watchdog: `/usr/local/bin/vpn-watchdog.sh` (cron */5 * * * *)

## Блокеры
- **Budget 1С live**: нужен доступ к бюджетным регистрам
- **DNS**: A-record `docs → 89.167.75.91` — Валентина делает сама
- **38 из 42** арендаторов не в IndParkDocs — contract-plan deviation ограничен

## Следующие задачи (приоритет)
1. ▶️ **Supplement card (ДС)** — стилизованная карточка (ТЕКУЩАЯ ЗАДАЧА)
2. Завершить: после создания договора → `showEntity(newId)` вместо `showEntityList`
3. Реализовать рендер contacts field
4. Мигрировать старые contact_person/phone/email → новый JSON формат
5. Date range filter UI для /finance
6. Добавить больше договоров аренды
