# 2026-03-03

## Последние коммиты (хронологически)
- `390b909` — act edit modal
- `e57a5dd` — acts in contract history
- `c3e9e0f` — company split (Наши/Сторонние/Из 1С)
- `b390c55` — 1С contract numbers in overdue
- `56880cb` — Должники expandable contractor rows
- `245f06b` — Расходы expandable contractor rows
- `d5a69ca` — searchable-select regex fix (`\d` → `\\d`)
- `df1ca68` — test suite (94 tests) + pre-deploy script
- `923aee7` — cadastral_number required for land_plot_part
- `48910cd` — rent object name fix (ЗУ → land_plot_part_name)
- `48aebf7` — finance page state preservation (_expOpenGids)
- `e9c740b` — equipment location on ЗУ ⚠️ СЛУЧАЙНО удалил ~450 строк живого кода
- `9791bc8` — supplement card DRY refactor
- `fcb93b4` — ci: deploy only after tests pass
- `a92c940` — fix: restore accidentally deleted live functions ← HEAD main
- `87104bf` — fix: inv_number in supplement card + equipment section restored
- `0bb5d94` — fix: inv_number in all collect/display points
- `7a2cc19` — docs: update MEMORY.md + FRONTEND_STRUCTURE.md ← HEAD dev

## Текущий HEAD
- `origin/dev` = `7a2cc19`
- `origin/main` = `a92c940`
- Feature branch `feature/ipd-supp-card-inv-number` = `0bb5d94` (PR #4 открыт → dev)

## Что сделано сегодня (2026-03-03)

### Ранее
1. ✅ Должники / Расходы — expandable contractor rows
2. ✅ Frontend разбит на 57 модулей в `server/src/frontend/`
3. ✅ searchable-select.js — regex fix
4. ✅ Тест-сьют — 94 tests, 8 suites
5. ✅ pre-deploy.sh — 7 проверок
6. ✅ Nav rename — "Расходы" + "Должники", убран DEMO badge
7. ✅ cadastral_number required — migration 024
8. ✅ Rent object name fix
9. ✅ Finance state preservation (_expOpenGids)
10. ✅ Equipment location on ЗУ (+ restore от удаления)
11. ✅ Supplement card DRY refactor (openSupplementCard → renderSupplementCard)

### Рефакторинг (6 фаз) — ЗАВЕРШЁН
- `entity-crud.js` — теперь 3-строчный STUB ("code moved to entities/ and forms/")
- Все функции вынесены в `server/src/frontend/entities/` и `server/src/frontend/forms/`
- `showEntity` → `entities/entity-detail.js:2`
- `showEntityList` → `entities/entity-list.js:80`
- `renderSupplementCard`/`openSupplementCard` → `entities/supplement-card.js`
- `renderContractCard` → `entities/contract-card.js`

### inv_number фиксы (PR #4: feature/ipd-supp-card-inv-number)
- `rent-objects.js` `collectEquipmentRentItems()`: сохраняет `inv_number` из `_equipment`
- `equipment-form.js` `getEqListValue()`: сохраняет `inv_number` из `_equipment`
- `routes/reports.js` `/contract-card/:id`: включает `inv_number` в eqList
- `entities/contract-card.js`: отображает `инв. XXXXX` рядом с именем
- `entities/supplement-card.js`: восстановлена секция "Передаваемое оборудование"

### inv_number состояние в БД
- id 580 (ЖД путь инв. №341000): `inv_number = '341000'` ✅
- id 581 (ЖД путь инв. №593000): `inv_number = ''` ⚠️ — нужно заполнить вручную
- id 582 (ЖД путь инв. №593100): `inv_number = ''` ⚠️ — нужно заполнить вручную

### Аудит полей форм (form fields audit) — В ПРОЦЕССЕ при компакте
Проверено:
- `collectRentObjectData()` — generic `.ro-field[data-name]` loop, всё собирается ✅
- `collectDynamicFieldValues()` — `getFieldValue(f)` по CONTRACT_TYPE_FIELDS + explicit fallbacks ✅
- `getFieldValue()` — handles act_items, equipment_list, advances, rent_objects, multi_comments, contacts, checkbox, select_or_custom ✅
- `_collectContacts()` — name/position/phone/email ✅
- `collectAdvances()` — amount+date ✅
- `getContractItemsValue()` — sale: name/qty/unit_price/amount; service: name/amount ✅
- `getActItemsValue()` — equipment_id, amount, description, comment, broken ✅
- `eqListCreateSubmit()` — inv_number, serial_number, year, manufacturer, status, category ✅
- `createEquipmentRentInline()` — inv_number + category ✅
- `_doSubmitEdit()` / `_doSubmitCreate()` — getFieldValue + collectDynamicFieldValues + collectEntityIds ✅
- `collectEntityIds()` — our_legal_entity, contractor_name, subtenant_name → id+name ✅

Ещё нужно проверить:
- `f_contractor_role_label` — рендерится как plain `<input>` в contract-form.js ~line 198-199; НЕ проверено входит ли в CONTRACT_TYPE_FIELDS
- `f_subtenant_name` — рендерится через renderSearchableSelect; проверить collectEntityIds
- `f_extra_services_cost` — referenced в recalcRentMonthly(); проверить CONTRACT_TYPE_FIELDS
- `f_vat_rate` — в renderEquipmentRentFields(); проверить CONTRACT_TYPE_FIELDS['Аренда оборудования']

## Структура frontend (57 модулей post-refactor)
```
server/src/frontend/
├── index.js                    — точка входа, сборка HTML
├── core.js                     — globals, API, CONTRACT_TYPE_FIELDS, icon()
├── core/utils.js               — escapeHtml, _fmtNum, _fmtDate
├── core/api.js                 — fetch wrapper
├── core/globals.js             — ENTITY_TYPE_ICONS, entityIcon
├── css.js                      — все стили
├── layout.js                   — sidebar, nav, shell
├── searchable-select.js        — searchable dropdown
├── components/amount-input.js, advances.js, contacts.js, duration.js,
│   contract-items.js, act-items.js
├── forms/field-input.js        — getFieldValue() by type
├── forms/equipment-form.js     — getEqListValue()
├── forms/land-plot-quick.js
├── forms/contract-form.js      — renderContractFormFields, collectDynamicFieldValues
├── entities/entity-helpers.js
├── entities/data.js
├── entities/entity-list.js     — showEntityList()
├── entities/entity-detail.js   — showEntity() at line 2
├── entities/entity-create.js   — _doSubmitCreate()
├── entities/entity-edit.js     — _doSubmitEdit()
├── entities/entity-delete.js
├── entities/contract-card.js   — renderContractCard()
├── entities/supplement-card.js — renderSupplementCard() + openSupplementCard()
├── modal.js
├── pages/nav.js, totp.js, legal-zachety.js, dashboard.js, finance-page.js, budget-page.js, map-page.js
├── rent-objects.js             — collectEquipmentRentItems() + collectRentObjectData()
├── entity-crud.js              — 3-line STUB
├── entity-form.js              — OLD (empty stub or contacts rendering)
├── reports.js                  — OLD (empty stub)
├── supplements.js, land-plot-parts.js, acts.js
├── reports/aggregate.js, pivot.js, linked-report.js, rent-analysis.js, work-history.js
├── relations.js, settings.js, ai-chat.js
```
**ОПАСНЫЕ конструкции:**
- `\'` внутри template literal → `'` (слеш съедается!) — использовать data-* или `\\'`
- `\d` внутри template literal → `d` — использовать `\\d`
- catch {} без параметра

## Вечер (19:10–19:20 UTC) — финальный блок

### Коммиты
- `341648d` — refactor: universal `_enrichFromRegistry` for all form data collection
- `41c15a3` — feat: supplement card — все изменённые поля для всех типов (Closes #3)
- `d6dfc72` — chore: fix minimatch vulnerability + raise eslint max-warnings to 80
- `4c6088e` — **Release: _enrichFromRegistry + supplement card fields** (Merge dev→main ✅)

### _enrichFromRegistry (341648d)
- Новая утилита `_enrichFromRegistry()` в `entities/entity-helpers.js` — единое место обогащения полей из реестров
- Заменяет 5 копий одной и той же логики в act-items, equipment-form, rent-objects, supplement-card, contract-card
- Включает `inv_number` фиксы (PR #4 superseded — больше не нужен)

### Supplement card (41c15a3)
- Карточка ДС теперь показывает все изменённые поля для всех типов договоров:
  - Общие: дата подписания, сумма, срок действия
  - Подряд/Услуги/Обслуживание: предмет + корпус
  - Таблица работ/услуг/товаров с суммами (contract_items)
  - Аренда/Субаренда: доп. услуги + таблица аренды оборудования (equipment_rent_items) с `_enrichFromRegistry`
- Закрыт issue #3

### Деплой (4c6088e)
- dev → main через merge, деплой прошёл ✅
- Текущий HEAD main: `4c6088e`

## Следующие задачи (приоритет)
1. **Заполнить inv_number** в БД: equipment ids 581 (→ '593000'), 582 (→ '593100')
2. **feature/ipd-auto-open-contract** — после создания договора → showEntity(newId) вместо showEntityList
3. **feature/ipd-contacts-field** — рендер field_type='contacts' в forms/field-input.js ~line 101
4. Миграция contact_person/phone/email → JSON contacts
5. Date range filter UI для /finance
6. ~~Merge PR #4~~ — superseded (inv_number fixes включены в `_enrichFromRegistry`)
7. ~~Merge dev → main~~ — СДЕЛАНО (4c6088e)

## Тесты и деплой
- `cd server && npm test` → 94 tests, 8 suites
- `bash scripts/pre-deploy.sh` от корня
- `systemctl restart indparkdocs` — деплой мгновенный
- Проверка JS: `cd server && node -e "const html = require('./src/frontend.js'); ..."`

## Блокеры
- **Budget 1С live**: нужен доступ к бюджетным регистрам
- **DNS**: A-record docs → 89.167.75.91 (Валентина делает)
- **inv_number 581/582**: пустые поля в БД

## AI Chat
- `routes/ai-chat.js` — Anthropic claude-haiku-4-5, SQL tool, 10 msg history
- ANTHROPIC_API_KEY в server/.env

## VPN
- ppp0 10.253.190.14; 1С: 192.168.2.3
- Watchdog: `/usr/local/bin/vpn-watchdog.sh` (cron */5)
