# 2026-03-07 — Сессия (начало ночи, UTC)

## HEAD на 00:09 UTC
- `origin/main` = после PR #136 (redesign parties block)
- Все PR задеплоены, 94 теста зелёные, сервис активен

## Выполнено за ночь (06.03 23:xx – 07.03 00:xx UTC)

### PR #130 — Fix ВГО логика
- Исправлена логика: ВГО только если ОБЕ стороны наши (our_legal_entity_id + contractor_id)
- Субаренда: + subtenant_id тоже наш
- Migration 036 пересчитал все договоры

### PR #131 — 5 полей объектов во все типы
- subject_buildings / subject_rooms / subject_land_plots / subject_land_plot_parts / equipment_list
- globals.js: все типы договоров обновлены
- Migration 037: добавлены field_definitions для act, order, letter
- field_type VARCHAR(20) → VARCHAR(50) (иначе 'subject_land_plot_parts' не влезало)

### PR #132 — Универсальный renderSubjectFieldsBlock
- field-input.js: SUBJECT_FIELDS[], renderSubjectFieldsBlock(props), collectSubjectFieldValues()
- acts.js: openCreateActModal + openEditActModal подключены

### PR #133 — Fix для нестандартных типов договоров
- Тип "Работы/Подряда" не совпадал с ключом → fallback без кнопок
- В DB: Работы/Подряда(1), Цессии(2), Электроснабжения(1), Процентного займа(1)
- renderDynamicFields fallback: добавлен renderSubjectFieldsBlock(props)
- collectDynamicFieldValues: всегда вызывает collectSubjectFieldValues() в конце

### PR #134 — Toggle-кнопки вместо развёрнутого списка
- renderSubjectFieldsBlock переписан: _saleSectionBtn паттерн (как в договорах)
- 5 кнопок в ряд, секции раскрываются по клику
- DESIGN.md создан — правила дизайна портала

### PR #135 — "Входит в" в inline-форме оборудования
- equipment-form.js: добавлен <select class="eq-create-parent"> в _renderEqListItem
- eqListCreateSubmit собирает parent_equipment_id
- В entity-create.js и entity-edit.js поле уже было (renderEqParentField)

### PR #136 — Redesign блока сторон в форме договора
- Весь блок (наша сторона + контрагент + ВГО) обёрнут в рамку
- Кнопка "поменять роли" → маленькая иконка ⇄ (34×34px) в 3й колонке ряда 1
- Тонкий разделитель между рядами
- ВГО: одна строка — checkbox + CSS-точка + текст без переноса

## DESIGN.md
- Создан /root/workspace-indparkdocs/DESIGN.md
- Правила: только Lucide (не emoji), CSS-переменные, компоненты, адаптивность
- **Backlog**: замена 117 emoji → Lucide в 32 файлах

## Технические баги найдены и исправлены
- Авторизация: не работала в обычном браузере — старый токен в localStorage (не cookies!)
  Решение: `localStorage.clear(); location.reload()` в консоли браузера
- Тип "Работы/Подряда" в DB — нестандартное значение, не совпадало с ключом в CONTRACT_TYPE_FIELDS

## Backlog (актуальный)
- **Замена emoji → Lucide** (117 в 32 файлах) — в DESIGN.md правила, ждёт отдельного PR
- **Breadcrumb навигация** — задача из скриншота Валентины, не реализована (только back button)
- **formatAmount** — решить: удалить из требований или алиас → Валентина должна решить
- **VPN keepalive** — настроить PPP keepalive (proposal готов)
- **4 старых договора Обслуживания** с полем building (строка) — нужно перезаписать в subject_buildings
- **inv_number** для eq id=581 (ЖД путь инв. №593000) — поле пустое

## Структура frontend (актуально)
- Унив. компоненты: renderSubjectFieldsBlock / collectSubjectFieldValues (field-input.js)
- SUBJECT_FIELDS[] — массив 5 полей объектов (там же)
- _saleSectionBtn / toggleSaleSection — toggle-паттерн для кнопок (sale-contract.js)
- Inline форма оборудования: _renderEqListItem (equipment-form.js) — теперь с "Входит в"

---

## Сессия утро 07.03.2026 (~08:00–09:07 UTC)

### Code Review (Codex) + исправления по всем пунктам

#### PR #137 — fix f_subject не сохранялся (Работы/Подряда)
- collectDynamicFieldValues не читал f_subject для нестандартных типов
- Добавлена явная сборка f_subject + contract_items из DOM при extraFields.length===0

#### PR #138 — Grouped equipment lists в searchable-select
- Оборудование в dropdown теперь группируется по equipment_category
- Без поиска: группы со sticky-заголовками; при вводе — плоский список
- Поля: equipment_rent, eq_parent, meter_equipment, act_equipment
- Добавлен .srch-group-hdr CSS класс

#### PR #139 — Убран чекбокс ВГО из формы
- Чекбокс f_is_vgo удалён — ВГО вычисляется автоматически на бэкенде
- Остался в карточках, таблицах, отчётах

#### PR #140, #141, #142 — Предмет договора для аренды
- Цепочка фиксов: сначала из rent_objects JSON, потом добавлен fallback через _rooms кэш
- PR #142: выяснилось что _rooms не загружен при первом рендере → добавлена предзагрузка
- **ПОЗЖЕ ЗАМЕНЕНО** на правильный подход из DB (PR #146)

#### PR #144 — Security fixes (code review раунд 1)
- letters.js: 4 роута без authenticate → добавлен auth
- auth.js: JWT_REFRESH_SECRET теперь отдельный, требуется в production
- Сгенерирован JWT_REFRESH_SECRET, добавлен в systemd
- finance.js: db → pool, console.error → logger.error
- Migration 039: btree индексы на contract_type, contractor_id, our_legal_entity_id, doc_status

#### PR #145 — Medium issues (code review раунд 2)
- entity-detail.js: удалён мёртвый код _allEntitiesForParent (грузил 200 сущностей на каждое открытие карточки)
- finance.js: ORG GUIDs (1С) вынесены в env (ORG_GUID_IPZ, ORG_GUID_EKZ)
- .env.example обновлён (JWT_REFRESH_SECRET, ORG_GUID)
- Новые тесты: finance (9), cube (9), floorplan (7) → 94→109 тестов

#### PR #146 — Предмет договора из DB (relations), не из JSON
- entities.js: добавлен subquery located_in_names (string_agg из relations WHERE located_in)
- entity-list.js: для Аренды/Субаренды показывает e.located_in_names из API
- Убрана вся JSON-логика (rent_objects) из колонки предмета
- Убрана предзагрузка _rooms

#### PR #147 — Migration 040: backfill located_in relations
- Для старых договоров где room_id/land_plot_id есть в rent_objects JSON но нет located_in связи
- Создаёт недостающие связи в DB

#### PR #148 — Fix: ambiguous column в migration 040
- SELECT id → SELECT e.id (JOIN с entity_types создавал неоднозначность)
- Сервис падал при старте (Startup error: column id is ambiguous)

### Итоги
- HEAD: origin/main = после PR #148
- Тесты: 109 тестов, 10 сьютов ✅
- Сервис: active ✅
- Из 14 договоров аренды 13 показывают предмет из DB. 1 пустой (040626 — помещение free-text без entity)
- Все пункты code review закрыты

### Системные изменения (вне кода)
- JWT_REFRESH_SECRET добавлен в /etc/systemd/system/indparkdocs.service
- ORG_GUID_IPZ и ORG_GUID_EKZ добавлены в systemd service
- systemctl daemon-reload выполнен

### Ключевые архитектурные решения
- **Предмет договора** — ВСЕГДА из DB-связей (located_in), не из JSON
- **ВГО** — только на бэкенде, в форме не показывать
- **Grouped selects** — при наличии category у объектов — группировать по category
- **JWT_REFRESH_SECRET** != JWT_SECRET — теперь требуется отдельно в production

### Backlog (актуальный после сессии)
- 040626/PRT-ИП-З-1 — пересохранить объект аренды через форму (помещение как entity)
- JSON→DB migration: rent_items, contract_line_items, act_line_items (большой рефактор)
- Breadcrumb навигация (скриншот от Валентины)
- Замена emoji → Lucide (117 в 32 файлах)
- inv_number для eq id=581 (ЖД путь инв. №593000)

---

## Сессия дневная 07.03.2026 (~09:07–15:24 UTC)

### PR #149 — Нормализация 5 JSON-массивов в реляционные таблицы (migration 041)
- `rent_items` (128 строк) из rent_objects
- `act_line_items` (24) из act_items
- `contract_line_items` (26) из contract_items/service_items
- `contract_advances` (4) из advances
- `contract_equipment` (179) из equipment_list/equipment_rent_items
- Хелперы в entities.js: `parseArr()`, `loadLineItems()`, `saveLineItems()`
- GET /api/entities/:id инжектирует массивы из новых таблиц в properties
- POST/PUT/PATCH сохраняют в новые таблицы; JSON в properties — резерв совместимости

### PR #150 — Нормализация field options в field_option_values (migration 042)
- `field_option_values(id, field_definition_id, value, sort_order)` — 71 строка, 14 полей
- entityTypes.js: `injectOptions()` / `saveOptions()` хелперы
- fd.options (jsonb) обновляется синхронно через saveOptions() для совместимости

### PR #151 — supplement contract_type наследуется от родителя (migration 043)
- field_definition id=35 (supplement→contract_type) удалена из DB
- `effective_contract_type` COALESCE(свой→родитель) в entities.js GET-запросе
- supplement-card.js, entity-list.js, entity-detail.js используют effective_contract_type

### PR #152 — AI chat timeout 30s → 90s
- AbortSignal.timeout(90000) в server/src/routes/ai-chat.js:80
- Причина: multi-step tool calls (AI→tool→AI) превышали 30s

### Написан SPEC_PRODUCT.md (235 строк, 9763 байт)
- Продуктовая спецификация в /root/workspace-indparkdocs/SPEC_PRODUCT.md
- 11 разделов: цель, 10 модулей, 17 типов сущностей, field definitions, relations, lifecycle, нормализованные таблицы, dropdowns, интеграции, роли, архитектурные принципы
- Живые счётчики из БД: company=428, meter=177, supplement=63, equipment=80, room=31, contract=44, act=13, building=15, land_plot=8

### SPEC_TECH.md — в процессе написания
- Собраны данные: 16 роутов, 46 миграций
- Ключевые таблицы: entities, entity_types, field_definitions, field_option_values, relations, relation_types, rent_items, act_line_items, contract_line_items, contract_advances, contract_equipment, ai_messages, ai_token_usage, audit_log, budget_data, users, refresh_tokens, legal_zachety, letter_topics, entity_files
- Интеграции: 1С OData, Claude Sonnet API

### IDENTITY.md — добавлено правило (JSON в properties)
- НИКОГДА не хранить массивы доменных объектов в JSON-строках внутри properties
- Исключения: floor_plans (SVG), map_points (UI-оверлей)
- f.options в field_option_values (теперь правильно в отдельной таблице)

## Итоговый HEAD (15:24 UTC)
- origin/main = после PR #152
- Тесты: 109 тестов, 10 сьютов ✅
- Сервис: active ✅

## Следующие задачи (после compact)
1. Дописать SPEC_TECH.md
2. Code review backlog (reports.js монолит, health.js IP allowlist, create-admin.js stdin)
3. Breadcrumb навигация
4. Замена emoji → Lucide (117 в 32 файлах)

---

## Сессия после compact (~15:55 UTC)

### ✅ ЗАВЕРШЁН рефакторинг: split reports.js + rent-objects.js

#### Backend split (reports.js → reports/)
- `server/src/routes/reports/helpers.js` — `_odataGetRpt`, `resolveRoArea`, `latestSuppValue`
- `server/src/routes/reports/pivot.js` — /pivot, /fields, /linked, /aggregate
- `server/src/routes/reports/rent.js` — /rent-analysis, /area-stats
- `server/src/routes/reports/work.js` — /work-history, /broken-equipment
- `server/src/routes/reports/contract-card.js` — /contract-card/:id, /advance-status
- `server/src/routes/reports/index.js` — монтирует все sub-routers
- Старый `reports.js` — удалён (или .bak)

#### Frontend split (rent-objects.js → rent-objects/)
- `server/src/frontend/rent-objects/shared.js` — globals, comments, calc fields
- `server/src/frontend/rent-objects/room-block.js` — room/land plot rent blocks
- `server/src/frontend/rent-objects/equipment-block.js` — equipment rent blocks
- `server/src/frontend/index.js` обновлён (rentObjectsShared + rentObjectsRoom + rentObjectsEquipment)
- Старый `rent-objects.js` — удалён

#### Результат
- 251 тест, 12 сьютов — все зелёные ✅
- JS синтаксис OK ✅
- Ветка: `feature/ipd-split-reports-rentobjects` (commit: 170d8ed)
- PR создан: https://github.com/ValentinaArta/IndParkDocs/pull/new/feature/ipd-split-reports-rentobjects

#### Следующие шаги
1. Смержить PR #153 (split refactor) — `gh pr merge --squash --admin`
2. Написать SPEC_TECH.md
3. Code review backlog (health.js IP allowlist, create-admin.js stdin)
4. Breadcrumb навигация
5. Замена emoji → Lucide (117 в 32 файлах)

#### Состояние main после merge
- 251 тесты ✅ (было 109 в записи в 15:24 — исправление: на самом деле 251)
- EnvironmentFile=/root/workspace-indparkdocs/.env (chmod 600)
- JWT_SECRET и JWT_REFRESH_SECRET в .env

---

## Сессия вечер 07.03.2026 (~16:36 UTC) — после compact

### PRs #154–#160 (из compact summary)

#### PR #156 — Удалены obsolete entity types workshop и crane_track
- Migration 044: DELETE FROM entity_types WHERE type_name IN ('workshop', 'crane_track')
- Все 28 ссылок в коде очищены
- Types had 0 entities in DB

#### PR #157, #158 — SPEC_PRODUCT.md обновлён
- #157: 19 собственных компаний с ИНН
- #158: структура полей доп. соглашений уточнена

#### PR #159 — Reports читают из нормализованных таблиц вместо properties JSON
- Все отчёты теперь читают из: rent_items, act_line_items, contract_line_items, contract_advances, contract_equipment
- getEffectiveSrc allowlist: dict {rent_items: 'contract_id', ...} — защита от SQL injection
- Effective source logic: latest supplement with rows → fallback to contract
- 251 тест, 12 сьютов ✅

#### PR #160 — Finance page: cache timestamp + VPN banner
- cacheGet() теперь возвращает {val, ts} вместо raw value
- Все 3 cached endpoints теперь включают cached_at: new Date(cached.ts).toISOString()
- _fmtCacheTs(iso) хелпер в finance-page.js → формат ДД.ММ.ГГГГ, ЧЧ:ММ
- Expenses badge: 'кэш 07.03.2026, 16:42' вместо статичного 'кеш 5 мин'
- VPN unavailable banner: когда d===null → ⚠ 1С недоступна — VPN не отвечает. Часть данных из кэша на ДД.ММ.ГГГГ ЧЧ:ММ.
- 251 тест, 12 сьютов ✅

### Расследование: пропавшие данные в карточке акта

**Проблема**: В карточке акта "Акт №26-О36-ТОИО от 2026-02-27" (entity id=890) в секции СВОЙСТВА
все поля (Статус, Корпуса, Помещения, Земельные участки, Оборудование) показывали "—"

**Причина**: PR #159 переключил чтение данных с `properties` JSON на нормализованные таблицы,
но для ТОИО-актов бэкфилл в act_line_items мог быть неполным.

**Данные в DB**:
- entity id=890 хранит act_items как **JSON-строку** внутри properties JSONB
- `properties->>'act_items'` = '[{"equipment_id":82,...}]' (stringified array)
- 4 акта c act_items: все с parent_contract_id "83" или "86"

**Статус**: расследование прервано compact. Нужно:
1. Проверить act_line_items WHERE act_id=890
2. Если пусто — написать backfill migration 045 для ТОИО актов
3. Или реализовать fallback в entity detail view

### Текущий HEAD (16:36 UTC)
- origin/main = после PR #160
- Тесты: 251 тест, 12 сьютов ✅
- Сервис: active ✅
- Миграции: 001–044 применены

### Backlog
- **Доделать расследование акта id=890** — проверить act_line_items, возможно migration 045 backfill
- **SPEC_TECH.md** — написать техническую спецификацию
- **Breadcrumb навигация** (скриншот от Валентины)
- **Замена emoji → Lucide** (117 в 32 файлах)
- **Code review backlog**: health.js IP allowlist, create-admin.js stdin

---

## Сессия поздний вечер 07.03.2026 (~16:36–17:36 UTC)

### PRs #162–#170 — Фиксы после нормализации + карточка акта

#### PR #162 — act_items renderer в entity-detail.js
- Добавлен хендлер act_items в fields.forEach loop
- Рендер таблицы оборудования на всю ширину (grid-column:1/-1)
- item.name || item.equipment_name (нормализованная таблица)
- subject_buildings/rooms/land_plots handlers для актов добавлены
- **Немедленно исправлено в PR #169** — использовалась необъявленная _entities

#### PR #163 — Поиск по полям-номерам
- В search query: act_number, doc_number, order_number, inv_number, meter_number, outgoing_number, room_number, cadastral_number
- Теперь поиск работает по всем идентификаторам сущностей

#### PR #164 — Fix форма редактирования акта (JSON.parse array)
- openEditActModal делал JSON.parse(array) → исключение → savedItems=[]
- Array.isArray() проверка перед JSON.parse для act_items и equipment_list

#### PR #165 — Fix JSON.parse на нормализованных массивах (9 мест в 5 файлах)
- entity-edit.js, entity-create.js, supplement-card.js, field-input.js, contract-card.js
- Везде Array.isArray() проверка перед JSON.parse()

#### PR #166 — Поиск по предмету договора
- Search query: subject, service_subject, located_in (из relations)

#### PR #167 — Карточка акта renderActCard()
- Новый файл act-card.js
- Header: номер, дата, статус badge, тип договора
- Ссылка на родительский договор; стороны из родителя (Заказчик/Арендодатель)
- Таблица позиций с зеброй и синей шапкой; заключение в зелёном блоке
- entity-detail.js: акты показывают renderActCard() + файлы

#### PR #168 — Fix кнопка "Создать" ДС (TypeError)
- _currentSuppExisting использовалась до объявления → TypeError → молчаливый сбой
- Добавлена var _currentSuppExisting = [] в начало модуля

#### PR #169 — Fix _entities в subject_buildings renderer
- Исправлен баг из PR #162: _entities не объявлена
- Заменено на: _buildings + _rooms + _landPlots + _landPlotParts (из data.js)

#### PR #170 — Карточка договора: актуальные условия по ДС
- entity-edit.js: ДС наследует contract_items/equipment_list/contract_type из родителя если пусто
- contract-card.js (бэкенд): добавлен cli_source_name в ответ
- contract-card.js (фронт): показывает '(из ДС №X)'; предупреждение если суммы не совпадают

### Итоги вечерней сессии
- HEAD: origin/main = после PR #170 (commit ff03184)
- Тесты: 251 тест, 12 сьютов ✅
- Сервис: active ✅
- Проблема с актом id=890 (ТОИО) — судя по тому что PR #167 добавил полноценную карточку акта, вопрос закрыт иначе (карточка показывает данные напрямую)

---

## Итоги дня (07.03.2026 — полный)
- Всего PRs за день: **#130–#170 = 41 PR**
- Тесты выросли: 94 → 251
- Миграции: 036–044 (9 новых)
- Ключевые достижения:
  - Нормализация данных: 5 JSON-массивов → реляционные таблицы
  - Security: auth, JWT_REFRESH_SECRET, индексы
  - Code review раунды 1 и 2 закрыты
  - Split refactor: reports.js + rent-objects.js → директории
  - Карточка акта (renderActCard)
  - SPEC_PRODUCT.md написан (235 строк)
  - Finance: cache timestamp + VPN banner
  - Поиск по всем полям-номерам и предмету договора

## Backlog на 08.03.2026
- **SPEC_TECH.md** — написать техническую спецификацию
- **040626/PRT-ИП-З-1** — пересохранить (помещение free-text → entity)
- **Breadcrumb навигация** (скриншот от Валентины)
- **Замена emoji → Lucide** (117 в 32 файлах)
- **inv_number** для eq id=581
- health.js IP allowlist, create-admin.js stdin

---

## Поздняя сессия 07.03.2026 (~17:00–23:00 UTC) — PRs #162–#170

### Баги и исправления (серия)
- **#162**: act_items [object Object] в карточке акта — хендлер был только в contract extraFields, добавлен в основной fields.forEach; subject_buildings/rooms/land_plots тоже добавлены
- **#163**: поиск по всем полям-номерам (act_number, inv_number, doc_number, order_number, meter_number, outgoing_number, room_number, cadastral_number)
- **#164**: форма редактирования акта не показывала сохранённое оборудование — JSON.parse(array) в openEditActModal и equipment_list родителя
- **#165**: JSON.parse(array) в 9 местах в 5 файлах после нормализации PR #149 — entity-edit, entity-create, supplement-card, field-input, contract-card (авансы, позиции, условия)
- **#166**: поиск по предмету договора (properties->>'subject', service_subject, EXISTS по located_in)
- **#167**: renderActCard() — новый файл act-card.js, карточка акта по образцу contract-card
- **#168**: кнопка "Создать" ДС не работала — _currentSuppExisting не объявлена, TypeError до try/catch
- **#169**: _entities не существует в subject_buildings рендерере — заменено на _buildings+_rooms+_landPlots+_landPlotParts
- **#170**: карточка договора — актуальные условия по ДС:
  - entity-edit.js: при редактировании ДС наследует contract_items+equipment_list из родителя
  - contract-card.js backend: cli_source_name в ответе API
  - contract-card.js frontend: "(из ДС №X)" в перечне позиций + предупреждение при расхождении суммы

### Ключевые выводы дня
- JSON.parse(array) — системная проблема после PR #149. Нужен везде Array.isArray guard
- _currentSuppExisting паттерн — модальные функции должны сохранять загруженные данные в глобальные переменные
- getEffectiveSrc — корректно работает, проблема была в данных (4 ДС без contract_line_items)
- 4 ДС требуют ручного обновления позиций: id=84, 115, 593, 909

### HEAD на конец дня (23:00 UTC)
- origin/main = после PR #170
- Тесты: 251, 12 сьютов ✅
- Миграции: 001–044 ✅
